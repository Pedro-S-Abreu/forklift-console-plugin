export enum MigrationType {
  COLD = 'cold',
  LIVE = 'live',
  WARM = 'warm',
}

export enum ProviderType {
  VSPHERE = 'vsphere',
  OVIRT = 'ovirt',
  OVA = 'ova',
  OPENSTACK = 'openstack',
}

export enum EndpointType {
  VCENTER = 'vcenter',
  ESXI = 'esxi',
}
